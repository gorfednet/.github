{
  "$comment": "Fleet-level findings — things wrong with the shared layer, or with a project that has not adopted the kit and so has nowhere of its own to record them. A project's own bugs belong in its own backlog once it adopts. Held by verification-kit/bin/check-backlog.mjs.",
  "entries": [
    {
      "id": "towit-broken-refs",
      "title": "towit.io could not fetch from GitHub at all, and had been failing silently",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "Work that had already merged looked unmerged in the local checkout, and any agent or person working there would have been editing against a stale picture of the remote without being told.",
      "evidence": "git fetch aborted with `fatal: bad object refs/remotes/origin/HEAD 2`. Five duplicate ref files under .git/refs/remotes/origin/ with a trailing ' 2' — the signature of a file-sync tool copying a directory it does not understand. Removing them restored the fetch, which then pulled d623370..a9f28a6 and showed the branch merged after all.",
      "pr": 3,
      "reviewBy": "2026-12-01",
      "note": "Recorded rather than just fixed because the class matters: a checkout that cannot reach its remote does not fail loudly, it works from stale refs. That is rule V27 wearing different clothes. Worth checking whether other repos under a synced directory have the same artefacts."
    },
    {
      "id": "towit-open-security-bump",
      "title": "towit.io has an open Dependabot PR bumping requests from 2.6.0 to 2.34.2",
      "status": "confirmed-bug",
      "assignee": "human",
      "userSymptom": "The site runs a Python HTTP client that is roughly a decade of releases behind, including security releases, on a repo whose owner had no way to see the PR because the local checkout could not fetch.",
      "evidence": "gh pr list on gorfednet/TowIt: PR #3 'Bump requests from 2.6.0 to 2.34.2', OPEN since 2026-09-01. A second, #1 for oauthlib 0.7.2 to 3.3.1, was closed rather than merged.",
      "reviewBy": "2026-10-15",
      "note": "Needs a human decision: 2.6.0 to 2.34.2 is not a drop-in on a Flask app of that age. Sized, not guessed at."
    },
    {
      "id": "fleet-audit-needs-a-token",
      "title": "The online half of the fleet check cannot run until FLEET_READ_TOKEN exists",
      "status": "confirmed-bug",
      "assignee": "human",
      "userSymptom": "Nothing is currently checking that a project claiming an adoption tier actually holds the files that tier requires, so the fleet table could drift back into fiction without any job going red.",
      "evidence": "check-fleet.mjs run under CI credentials reports every private sibling as unreadable, because a workflow's default GITHUB_TOKEN is scoped to its own repository. fleet-audit.yml exists and fails closed on the missing secret; the pull-request job runs --offline and says so in its output.",
      "reviewBy": "2026-10-15",
      "note": "Wants a fine-grained PAT, read-only Contents + Metadata across gorfednet, stored as the repository secret FLEET_READ_TOKEN. Until then the weekly job is red on purpose: unverified is the true state, and a green job that checked nothing is worse. The cause of the original mis-report is now rule V39."
    },
    {
      "id": "fleet-adoption-outstanding",
      "title": "Twelve of fourteen projects had the verification gate switched off",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "A bug shipped by any of those twelve projects has nothing checking that the CI which passed actually examined anything, which is how the sixteen findings in the BinderCurve teardown stayed green for months.",
      "evidence": "fleet.json: 12 projects at tier 0. Each already inherits a reusable pr-check workflow, so adoption is a one-line change plus the vendored kit.",
      "reviewBy": "2026-11-01",
      "note": "Landed in #14 and the rollout around it; fleet.json now holds no project at tier 0. The entry claimed twelve at tier 0 for weeks after that stopped being true, which is the class --verify-prs catches for pull requests and nothing catches for a count. What remains is not adoption but five projects claiming a tier whose floor never ran — tracked separately as five-projects-run-tests-with-no-floor.",
      "pr": 14
    },
    {
      "id": "unbounded-jobs-in-shared-workflows",
      "title": "Three jobs in the org repo could run for six hours, one of them inherited by six projects",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "A hung CI job would have kept a runner busy for six hours and billed for it, with no red check to say so — the projects calling browser-compat.yml would just have seen a check that never finished.",
      "evidence": "check-ci-headroom reported \"no timeout-minutes found\" on gorfed.net and subrythm.com. Auditing the workflows those repos call found compat-success (browser-compat.yml, 6 callers), audit (fleet-audit.yml) and kit (verification-kit.yml) with no timeout-minutes. GitHub kills an untimed job at six hours.",
      "pr": 5,
      "reviewBy": "2026-12-01",
      "note": "Fixed and guarded: test/orgWorkflows.test.mjs now requires a timeout on every job that does not delegate with uses:. Guard observed failing by deleting the new timeout from verification-kit.yml. Marked landed 2026-09-10, on the reconciliation --verify-prs now performs: the entry had read in-review since the pull request merged, which is the state that check was written for."
    },
    {
      "id": "headroom-blind-to-matrix-jobs",
      "title": "The headroom check could not see a matrix job at all",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "A slow matrix job — the kind most likely to be near its limit — would never have been reported, and the check would still have printed a green tick.",
      "evidence": "On bindercurve.com, coverage was 38% of declared jobs. GitHub reports the interpolated name (e2e-chromium-1) for a templated name:, which matches neither the template nor the job id, so no matrix job ever joined to its timeout. Two workflows both declaring a job named 'check' also collapsed into one map entry, measuring one against the other's limit.",
      "pr": 4,
      "reviewBy": "2026-12-01",
      "note": "matchJob turns the template into a pattern, requires at least one literal character so a name that is only an expression cannot claim every job, and resolves by specificity. Coverage went 38% -> over the 80% floor. Marked landed 2026-09-10, on the reconciliation --verify-prs now performs: the entry had read in-review since the pull request merged, which is the state that check was written for."
    },
    {
      "id": "backlog-status-never-checked-against-github",
      "title": "An in-review entry stays in-review forever after its PR merges",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "The backlog is meant to be the one place you can read to know what is actually open. Every entry marked in-review keeps reading as in flight after its pull request merges, so the list overstates what is outstanding and understates what shipped — which is the exact failure a committed registry exists to prevent.",
      "evidence": "check-backlog.mjs requires an in-review or landed entry to name a pull request, and never asks GitHub what became of it. Confirmed by grep: nothing in verification-kit/lib/backlogSchema.mjs touches gh or the API. Twelve adopted repositories now carry entries in that state, wychwoodresort.com's two from the moment PR #4 merged.",
      "reviewBy": "2026-11-01",
      "note": "The check has to keep working offline and in a fresh clone, so this cannot be unconditional. An opt-in --verify-prs used by the CI gate, where gh and a token are present, fails closed when the API is reachable and disagrees, and skips with a counted, printed reason when it is not. Batch it with the venv entry below into one kit version rather than re-vendoring twelve repositories twice.",
      "pr": 18
    },
    {
      "id": "citation-scan-walks-untracked-third-party",
      "title": "The citation check reads code the project did not write",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "A project can be told it cites a rule that does not exist, by a file it did not write and cannot edit. That reads as a broken check rather than a finding, and the response it invites is to switch the check off.",
      "evidence": "check-rule-citations.mjs skips .venv but not venv, which is the commoner name and is present unignored at towit.io's root — so the walk descends into a whole third-party site-packages tree. Separately, its citation pattern allows any whitespace including newlines between the word and the number, so a wrapped line in a minified bundle reads as a citation: towit.io's vendored AngularJS produced a citation of a rule numbered zero.",
      "reviewBy": "2026-11-01",
      "note": "Adding vendored paths to the kit's skip list is the wrong fix, since it weakens the check everywhere for one repository's layout — PR #13 exposed rule-sources so a project can scope its own scan instead (V54). Still worth adding venv beside .venv, and worth deciding whether the whitespace class should exclude newlines; a citation genuinely wrapped across two lines of prose is plausible, so measure before tightening.",
      "pr": 18
    },
    {
      "id": "tier-2-evidence-proves-nothing",
      "title": "A project can claim tier 2 without any executed-count assertion running",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "FLEET.md is the one place to read what each project is actually protected by. Tier 2 says an executed-count assertion is wired to a real test report, and the registry check will confirm that claim for any project that has merely vendored the kit — so the column can overstate protection while the check that guards it stays green.",
      "evidence": "TIER_EVIDENCE[2] in scripts/check-fleet.mjs asks for verification-kit/bin/assert-tests-executed.mjs, which ships in every copy of the kit and is therefore present in every tier-1 project. Six projects now claim tier 2; the online run confirmed all six against a file that proves nothing about wiring. Presence read as behaviour, which is the class V20 names.",
      "reviewBy": "2026-11-01",
      "note": "Real evidence is in the caller: fetch .github/workflows/*.yml and require the pr-check job to pass a non-empty test-report together with a min-tests above the default of 1. The check already fetches file contents over the API, so this is a content assertion rather than new machinery. It has to keep its tri-state — an API failure must not read as absent. Note the honest awkwardness this exposes: TowIt has a real floor of 49 that no shared assertion can see, because pytest emits no JSON report either check could read, and it is recorded at tier 1 for that reason.",
      "pr": 18
    },
    {
      "id": "drift-gate-treats-behind-as-broken",
      "title": "Improving the shared kit turns every project red until thirteen pull requests land",
      "status": "landed",
      "assignee": "human",
      "userSymptom": "Fixing one bug in the kit means opening, reviewing and merging thirteen mechanical pull requests, and until the last one lands every other project's CI is red for a reason that has nothing to do with its own code. Measured twice in one night: v0.25.0 and v0.26.0 each cost thirteen.",
      "evidence": "check-kit-drift.mjs fails when a vendored copy does not match the canonical manifest, without distinguishing the two ways that happens. A copy that is *behind* is intact and self-consistent; a copy that has been *edited locally* is a fork pretending to be the shared kit. Only the second is a defect, and the first is the ordinary state of a fleet between an upstream fix and its rollout. Both currently exit 1 with the same instruction.",
      "reviewBy": "2026-10-15",
      "note": "The fix is a judgement call about how much staleness is tolerable, which is why it is assigned to a person rather than taken tonight. The shape that seems right: compare the vendored version against canonical and fail only on local modification or on being more than one minor version behind, warning in between, so a project can merge its own work the day an upstream fix lands and pick the refresh up on its own schedule. That must not become an excuse for a copy drifting for months — a dated ceiling, per the deferral rule, not an open-ended warning. Worth weighing against the alternative of not vendoring at all: a git submodule or an npm package would make the version a single edit, at the cost of the property that made vendoring attractive, which is that every check runs on plain node with nothing installed.",
      "pr": 18
    },
    {
      "id": "five-projects-run-tests-with-no-floor",
      "title": "Five projects run a test suite in CI with nothing asserting how much of it ran",
      "status": "landed",
      "assignee": "main-model",
      "userSymptom": "A suite that silently stops running looks exactly like a suite that passes. On gorfed.net, denseware.com, gorfmusic.com, ssatcy.com and wychwoodresort.com a spec-selection mistake, a renamed directory or a crashed reporter would take the coverage away and leave the check green, because no step compares the number of tests that executed to anything.",
      "evidence": "Each of the five calls the shared pr-check workflow with a min-tests value and no test-report. verification-gate/action.yml runs the assertion under 'if: inputs.test-report != ''', so the floor they configured has never executed once. Found by the tier-2 content assertion added in this change, which reported all five; their FLEET.md tier is corrected to 1 in the same commit, because the registry must not claim a protection that is not running.",
      "reviewBy": "2026-10-15",
      "note": "The fix per repo is to emit a JSON reporter alongside the existing run and pass it as test-report with a floor measured from a real green run, then raise the tier back to 2 in fleet.json and let the online audit confirm it. Doing that during the v0.27.0 roll keeps it to one PR per repository. wychwoodresort.com is out of scope for site work by the owner's instruction, so it stays at its true tier rather than being wired.",
      "pr": 18
    },
    {
      "id": "drift-read-its-authority-through-a-cdn",
      "title": "Two consumers failed as \"ahead of canonical\" three minutes after a release, because the check read a cached manifest",
      "status": "landed",
      "assignee": "main-model",
      "pr": 19,
      "userSymptom": "A red build with a message saying something impossible: that a site is ahead of the kit it vendors. Nobody can act on that, and the next red one gets less attention.",
      "evidence": "v0.27.0 merged at 03:49:36Z. subrythm.com and blackpixelrecords.com ran at 03:52:32Z and 03:52:52Z and read v0.26.0 from raw.githubusercontent.com, which is CDN-cached for minutes, while the API and the CDN both read 0.27.0 by the time it was checked by hand. distance() correctly called the vendored copy ahead and the check correctly treated that as fatal; the input was wrong. Fixed by confirming an impossible answer against an uncached API read before acting on it, and failing when that read cannot be obtained. Three cases, watched failing first, plus canary drift-believes-a-cached-read. Recorded as V60.",
      "note": "These two were the only repositories of thirteen that run check-kit-drift at all, so the fleet had been reading the absence of this check as health. Wiring it everywhere is part of the v0.28.0 roll."
    },
    {
      "id": "two-matchers-fired-on-their-legitimate-neighbour",
      "title": "The flake detector reported expected failures, and the citation matcher read a rulebook section as a rule number",
      "status": "landed",
      "assignee": "main-model",
      "pr": 19,
      "userSymptom": "Two checks reporting things that are not true. A suite with a test.fail() case would be described as intermittent, and with a --max-flaky budget would fail over tests behaving exactly as written; a comment naming a Digimon rulebook section would be reported as citing a rule that does not exist.",
      "evidence": "Bugbot found the first on nine consumer pull requests simultaneously: assert-tests-executed counted any failed attempt as a retry, and Playwright marks test.fail() as expected with a failed attempt. The same line also missed timedOut-then-passed, the commonest intermittent shape there is. The second was found by running check-rule-citations against bindercurve.com, where it is vendored and wired to nothing: the prefix class [A-Z0-9]+ accepted a bare digit, so \"comprehensive rules 1-2-1-1-1\" read as local rule 1-2. Both are V10 instances and are recorded there as evidence rather than as new numbers. Three cases watched failing first, three canaries.",
      "note": "Neither was found by rereading the pattern. One came from an outside reviewer, the other from running the check in a repository that had never run it, which is the argument for both."
    },
    {
      "id": "citation-matcher-read-a-zero-as-a-rule",
      "title": "A vendored AngularJS comment about pluralization read as citing rule 0",
      "status": "landed",
      "assignee": "main-model",
      "pr": 20,
      "userSymptom": "A check reporting a rule that does not exist, in a file nobody here wrote. towit.io does not wire this check, so like bindercurve’s rulebook section it was a false failure waiting for whoever wired it first — and the reasonable conclusion on seeing it would be that the check is noise.",
      "evidence": "Reported by the batch that rolled v0.27.0: towit-admin/static/js/angular/angular.js and its angularjs twin, \"we added three explicit number rules 0, 1 and 2\". No sequence starts at zero, so a number beginning with 0 is prose by definition; the matcher now reads [1-9]\\d* in all three alternatives. Watched failing first, canary citation-reads-a-zero-as-a-rule, and verified against the real towit.io tree that produced it: 91 source files, every citation resolves. Third instance of V10 in two releases, which is why it is recorded there with the note that narrowing a matcher changes behaviour in both directions — the previous narrowing broke 4thcltr’s real 4C- prefix."
    },
    {
      "id": "metadata-gate-flagged-correct-markup",
      "title": "The metadata gate reported a defect in a URL that was written correctly",
      "status": "in-review",
      "assignee": "main-model",
      "pr": 32,
      "userSymptom": "A red check naming a bug that is not there, on a page whose social card works. The reasonable response is to go and 'fix' correct markup, which is what nearly happened: the finding was carried into a plan and into a delegated task as established fact, and an agent was briefed to expect the failure and repair it.",
      "evidence": "check-page-metadata.mjs: `const doubleEscaped = (raw) => /&amp;|&#38;|%26amp%3B/i.test(raw)` tested the raw attribute. `&amp;` is how `&` is written in an attribute value, so every correctly escaped multi-parameter URL failed. Its own docstring named the right test, 'an entity surviving one decode', and the code never decoded. Settled by fetching rowanmcarthur.com's og:image with the new live checker: the crop applies and the image is 1200x630.",
      "reviewBy": "2026-12-01",
      "note": "Fixed by decoding once and looking for a survivor, which required decodeEntities to become a single pass - a chain of replaces turns `&amp;lt;` into `<`, so escaped-once and escaped-twice come out identical and the distinction the caller needs is gone before it can be measured. Recorded as a fourth V10 instance. The part that generalises is not the pattern but the protection: a case in the suite asserted the same wrong thing, so checker and test confirmed each other and the suite stayed green. A case watched failing proves the assertion runs, not that it is right - and a near-miss test written by whoever held the wrong belief is worth nothing. When a check reports a defect in something you did not write, observe the thing itself."
    },
    {
      "id": "card-replaced-under-a-cached-name",
      "title": "A social card can be fixed in the repository and stay broken on the internet for a year",
      "status": "in-review",
      "assignee": "main-model",
      "pr": 32,
      "userSymptom": "Sharing a link keeps showing the old, wrong card long after it was fixed and deployed, and every check says the site is correct. Nobody can find the fault, because there is no fault left in the repository.",
      "evidence": "The vhost template on dapyllil serves image, css and js extensions with `expires 1y` and `add_header Cache-Control \"public, immutable\"`. Confirmed live: `cache-control: public, max-age=31536000, immutable` on favicon.svg across five hosts. ssatcy.com/og-image.png and 4thcltr.com/og-image.png both already existed under that header, one of them jpeg bytes at 768x1024. check-og-image reads the publish set, so a card replaced under its old name is green in the repository, green in the build and stale at the URL.",
      "reviewBy": "2026-12-01",
      "note": "Two halves. Structurally, a new card lands at a new URL rather than overwriting the old name - 4thcltr.com already does this with `?v=3` and is the pattern to copy. Verification-wise, check-live-og-image.mjs fetches the page, reads the og:image a scraper would read, fetches that, and compares the served bytes against the page's own declared dimensions; a declared size the URL does not serve is the staleness signature. It also reports an immutable Cache-Control as a note, so the next person replacing a card is told why it will not reach anyone. The class is general: any check that reads the artefact a build produced cannot see what a cache in front of it is still serving."
    },
    {
      "id": "guard-failed-on-its-own-explanation",
      "title": "A guard blocked a repository for documenting why it obeys the guard",
      "status": "in-review",
      "assignee": "main-model",
      "pr": 32,
      "userSymptom": "A pull request red on a check naming a file that does nothing wrong, and the check cannot be satisfied by any wording - the deploy script explains in a comment why it does NOT use the forbidden mount, and saying so is what trips it. The only ways out are to delete the explanation or to ignore the check.",
      "evidence": "scripts/no-smb-guard.sh scanned one pattern, smb://|mount_smbfs|\\bcifs\\b|/Volumes/, over prose and code alike, with a single-file exclusion for docs/verification-rules.md. ssatcy.com CI, step No legacy share-mount deploy paths: matched a comment in its deploy script. Because that step precedes Build, the skipped build then gave the verification gate no dist/ to read and it emitted 18 ENOENTs under !cancelled().",
      "reviewBy": "2026-12-01",
      "note": "Split into two patterns. ACTIONABLE (smb://, mount_smbfs, mount -t cifs, /Volumes/) is copy-pasteable and fails anywhere including inside a comment; the bare filesystem word fails only on lines that are not comments. Four cases pin the directions the allowance must not open, and all were watched red under mutation. The deeper finding is V69: the awk filter added to drop comment lines was rejected by awk, printed nothing, and the guard reported a clean tree - a broken filter emits exactly what a clean scan emits, and the || true absorbing the scanner no-match status swallowed the difference. Its own new test caught that, before CI did. The filter is now fatal and exits 2, so a tooling fault cannot read as a pass or as a finding."
    },
    {
      "id": "gate-buried-the-cause-it-outlived",
      "title": "The check designed not to be silenced became the loudest noise in the log",
      "status": "in-review",
      "assignee": "main-model",
      "pr": 32,
      "userSymptom": "A failed run whose log is eighteen missing-file errors, every one of them real, none of them the reason. The actual failure is a screen and a half higher and reads as unrelated, so the diagnosis goes to the loud true errors first.",
      "evidence": "ssatcy.com run: the share-mount guard failed, Build was skipped, and check-page-metadata, check-og-image and check-error-pages each reported ENOENT for every page and file they wanted in a dist/ that had never been produced. V18 puts !cancelled() on every gate step deliberately, so this is the cost of a correct decision rather than a bug in it.",
      "reviewBy": "2026-12-01",
      "note": "Recorded as V70. Fixed with a precondition step in the verification-gate action that fails once when a root it was told to read is absent or empty, and names the likely cause - look for the first failed or skipped step; if Build did not run, that is why. It does not skip: a genuinely missing publish set is still a failure, now a legible one. Guarded by five cases that execute the step’s extracted shell rather than matching its text, since the question is what the script decides; five mutations watched red, including one that renamed the step, which proves the extraction helper throws instead of handing every case an empty script."
    }
  ]
}
